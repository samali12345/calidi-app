// ⚠️ Update this IP to your Mac's local IP address if it changes
// Run: ipconfig getifaddr en0   (on your Mac terminal)
export const API_BASE_URL = 'https://calidi-app-production.up.railway.app/api';
export const IMAGE_BASE_URL = `${API_BASE_URL}/products/image`;
